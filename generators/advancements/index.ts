import { dataDir } from "../../constants";

export const format = "Using base \"%s\" with generator \"%s\" to provide advancement for \"%s\".";
export const outDir = new URL("advancements/", dataDir);
