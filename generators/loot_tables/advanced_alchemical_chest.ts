import {genericColor} from "../../util";

const BASE = new URL("advanced_alchemical_chest.json", import.meta.url).pathname;
export default genericColor.bind(null, "advanced_alchemical_chest", BASE, []);
