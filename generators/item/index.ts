import { assetsDir } from "../../constants";

export const format = "Using base \"%s\" with generator \"%s\" to provide item model for \"%s\".";
export const outDir = new URL("models/item/", assetsDir);
