import { assetsDir } from "../../constants";

export const format = "Using base \"%s\" with generator \"%s\" to provide block model for \"%s\".";
export const outDir = new URL("models/block/", assetsDir);
