import { dataDir } from "../../constants";

export const format = "Using base \"%s\" with generator \"%s\" to provide loot table for \"%s\".";
export const outDir = new URL("loot_tables/blocks/", dataDir);
