const { assetsDir } = require("../../util");

module.exports.format = "Using base \"%s\" with generator \"%s\" to provide block model for \"%s\".";
module.exports.outDir = `${assetsDir}/models/block`;
