import { assetsDir } from "../../constants";

export const format = "Using base \"%s\" with generator \"%s\" to provide blockstate for \"%s\".";
export const outDir = new URL("blockstates/", assetsDir);
